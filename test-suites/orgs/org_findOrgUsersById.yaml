name: org_findOrgUsersById

authors:
  - Asiya Shaji

type: SUITE

init:
   - create_org_abstract

endpoint: /orgs/{{@create_org_abstract_Response.data.id}}/users

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"

severity: Major

category: Functional

tags:
    - V2

policies:
    logger: DEBUG
