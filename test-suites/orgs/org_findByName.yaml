name: org_findByName

authors:
  - Asiya Shaji

type: SUITE

init:
   - create_org_abstract

endpoint: /orgs/find-by-name/{{@create_org_abstract_Response.data.name}}

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"

severity: Major

category: Functional

tags:
    - V2

policies:
    logger: DEBUG
