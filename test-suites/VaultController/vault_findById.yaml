name: vault_findById

authors:
  - Asiya Shaji

type: SUITE

init:
   - create_org_abstract
   - create_vault_abstract

endpoint: /vault/{{@create_vault_abstract_Response.data.id}}

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"

cleanup:
  - delete_vault
  - delete_org

severity: Major

category: Functional

tags:
    - V2

policies:
    logger: DEBUG
