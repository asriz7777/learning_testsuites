name: project_checksums

authors:
  - Asiya Shaji

type: SUITE

init:
   - create_githubacc_abstract
   - create_githubproj_abstract

endpoint: /projects/{{@create_githubproj_abstract_Response.data.id}}/project-checksums-all

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"

cleanup:
    - delete_githubproj
    - delete_githubacc

severity: Major

category: Functional

tags:
    - V2

policies:
    logger: DEBUG
