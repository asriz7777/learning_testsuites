name: job_findby_id

authors:
  - Asiya Shaji

type: SUITE

#init:

endpoint: /jobs/{{@create_job_abstract_Response.data.id}}

method: GET

auth: Default

assertions:
  - "@StatusCode == 200"
  - "@Response.errors == false"
  - "@Response.data != EMPTY"


severity: Major

category: Functional

tags:
    - V2

policies:
    logger: DEBUG
